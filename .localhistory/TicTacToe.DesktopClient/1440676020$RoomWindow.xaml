<Window x:Class="TicTacToe.DesktopClient.RoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RoomWindow" Height="453" Width="431">
    <Grid>
        <ListBox Name="ListBoxAvailableGames" HorizontalAlignment="Left" Height="230" Margin="71,103,0,0" VerticalAlignment="Top" Width="304">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Label Content="Available games" FontSize="25" HorizontalAlignment="Left" Margin="129,29,0,0" VerticalAlignment="Top" Width="192"/>
        <Button Content="Create game" HorizontalAlignment="Left" Margin="237,364,0,0" VerticalAlignment="Top" Width="138" Height="36" Click="Button_ClickCreateGame"/>
        <Button Content="Join game" HorizontalAlignment="Left" Margin="71,364,0,0" VerticalAlignment="Top" Width="138" Height="36" Click="Button_ClickJoinGame"/>

    </Grid>
</Window>
